# 资产配置月度监测历史导入

`scripts/import_monthly_monitor.py` 从本地月度监测工作簿的“资产明细表”流式提取数据，按 A 列的真实日期拆分，写入现有 Parquet 快照库。

## 本次覆盖

- 来源：`data/1 资产配置月度监测_v2026.xlsx`，129,614,083 字节、42 个工作表。
- 明细表：187,150 行（含首行表头及 20 行分块表头）；实际业务记录 187,129 行。
- 源数据期间：2024-12-31 至 2026-08-31，连续 21 个月末。
- 新增：2024-12-31 至 2026-02-28，共 15 个快照、122,542 行。
- 保留：原有 15 个快照，包括源表重叠的 2026-03 至 2026-08 月末，以及全部月中快照。
- 完成后：30 个时点、329,849 行，最新仍为 2026-09-10。

## 必须保留的口径说明

1. **监测表和宽表覆盖不同。** 重叠月份显示监测表对资管公司产品、资管自营、华泰财险委托等范围覆盖不完整。以 2026-03-31 为例，监测表为 7,647 行、6,908.685284246 亿元，原宽表为 8,261 行、7,092.697085106 亿元；二者均包含源表负债及层级记录，不能直接等同于应用去重后的投资资产规模。旧快照不被替换，跨来源规模变化包含覆盖差异。
2. **缺少年初市值。** 源表提供 24 个标准业务字段，缺少 `年初市值(亿)`；导入值保持空值，并在 manifest 中记录 `missing_fields`。页面“年初以来”在缺少该字段时改用上一年 12 月 31 日正式快照：2025 年各月对比 2024-12-31，2026 年 1、2 月对比 2025-12-31。按账户、品种等维度分别汇总两端数据，纳入仅年末存在的持仓；收益与平均资金占用仍取当前源表本年以来累计值。基准日期在页面标明，覆盖差异仍会影响变化金额；不回填或改写源数据。2024-12 因缺少 2023 年末基准，仍不提供年初持仓比较。已有源表年初市值的月份继续优先使用原字段。
3. **保留业务记录。** 只删除逐字段确认的重复表头，不聚合、不去重，不删除已退出资产、零余额、回购负债或未分类业务行。一般缺失标签及久期按原加载器规则标准化。
4. **报表口径只用于校验。** “全量-时序”从 2026-04 起排除“穿透账户”；分类辅助列的错误值不作为业务资产分类。原始持仓保留所有层级，应用沿用已有层级选择规则。
5. **年度计划按年份使用。** 当前仅配置 2026 年收益计划；2024/2025 时点不会套用 2026 年计划。

## 可复现导入

依赖项目 `requirements.txt`，大表 XML 读取使用 `lxml`，无需启动 Excel 或刷新外部链接。

```powershell
python -X utf8 scripts/import_monthly_monitor.py "data/1 资产配置月度监测_v2026.xlsx"
```

查看 `output/monthly_history/import_report.json`。每个日期记录新增或保留动作、记录数、金额，以及与现有快照的差额。确认报告后应用：

```powershell
python -X utf8 scripts/import_monthly_monitor.py "data/1 资产配置月度监测_v2026.xlsx" --apply
```

脚本先核验日期、表头、数值及 1,260 项源报表控制数，再将新增快照写入临时目录并回读比对。所有检查通过后才更新正式 manifest；已存在日期始终保留。再次执行只报告重叠，不重写旧文件。

溯源使用原工作簿文件名、SHA-256、“资产明细表”、A 列日期和原 Excel 行号。多日期工作簿通过 `source_kind=monthly_monitor` 识别，原有按文件名日期识别的宽表规则保留。

## 本次验证

- 1,260 项源报表核对通过；最大金额差为浮点舍入量级，约 `1.14e-11` 亿元。
- 新增快照逐行回读比对通过；15 个历史月份的记录数和三项金额有固定回归控制。
- Parquet-only 加载：30 个时点、329,849 行，`errors=[]`。
- 原有 15 个 Parquet 文件及 manifest 条目与导入前完全一致。
- 全部 113 项单元测试通过，包含 14 个历史月份在账户、品种、委受托、账户内品种四种维度下的年初基准及累计收益核对；新增、退出持仓与源表年初字段优先规则均通过。
- 首次导入时的 Streamlit AppTest：最新时点、2025-01、2026-02、2026-03、最早 2024-12，均无页面异常；最早时点自动采用时点对比，跨来源比较显示覆盖提示。
- 本地服务首页及 `/_stcore/health` 均返回 HTTP 200。
- 浏览器实际切换至 2025-01-31，确认单月复盘、2024-12-31 基准及 6,983 行显示正确；临时校验服务已关闭，正常服务保留在 `http://127.0.0.1:8501/`。
- 年初比较修正后，在 8501 实际确认 2025-12-31、2026-01-31、2026-02-28 正式版均可使用“年初以来”，基准分别为 2024-12-31、2025-12-31、2025-12-31。

原始 Excel 和临时提取资料仅留本地。此次完成本地数据及代码更新，尚未提交或推送 Git。
