# Copy this file to `.streamlit/secrets.toml` for local use, or set the same
# key in your deployment platform's Streamlit secrets panel.
#
# Do not commit a real production password.
app_password = "replace-with-a-strong-password"
